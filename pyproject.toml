[project]
name = "iina-live"
version = "0.1.0"
description = "解析直播平台(虎牙/抖音/斗鱼/B站)的真实直播流并用 IINA/mpv 播放，修复 flv 约 2 分钟断流"
readme = "README.md"
requires-python = "==3.14.*"
# 仅使用 Python 标准库，无第三方依赖
dependencies = []

[tool.uv]
# 单包 iina_live，入口 python -m iina_live;不作为可安装包构建，uv 只负责解释器/环境
package = false
